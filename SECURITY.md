# 安全策略

## 报告漏洞

**不要开公开 issue。**

走仓库的 **Security** 标签页 → 「Report a vulnerability」，私密提交。
（那个入口用不了的话，开一个 issue 但**只写**「我想报告一个安全问题」，
别写细节，我会回你一个私密渠道。）

## 先看这里，能省一次来回

这是个**本地跑的 CLI**：没有服务端、没有账号体系、不监听任何端口、不接收外部请求。
「谁能访问它」这个问题不存在 —— 能执行它的人，本来就能读你机器上的文件。

所以下面这几类报告**不算漏洞**，看到请略过：

- **「`--from` / `--out` 能读写当前目录以外的文件」**
  路径是**运行这条命令的人自己给的**，没有跨信任边界。把它收在「只允许当前目录内」
  是为了防「从聊天记录里复制一条长命令」的手滑，不是用来防攻击者的。

- **「token 出现在发给 Steam 的 URL query 里」**
  这是 Steam 家庭组接口的协议要求，改不了。它确实会被会解密 HTTPS 的
  代理 / 杀软 / 公司网关看到 —— 别在不可信的网络里用它（README 里也写了）。

- **「报告 HTML 里有成员的真实信息」**
  那是它的功能。报告是给你自己看的，**别转发到家庭组以外** —— 这是使用者的责任。
  相关的用法提醒在 README 的「隐私与凭据」一节。

## 真正想看的

- 渲染层被注入：某个字段绕过转义进了 `innerHTML`，或者头像 URL 逃出白名单前缀；
- 报告产物里出现了原本不该落盘的凭据；
- 依赖或构建链路被投毒（本项目零运行时依赖，所以这一条的攻击面很小）；
- `tests/docs.test.js` 这道发布闸门能被绕过。

## 支持范围

只维护 `main` 上的最新版本，旧版本不回补。
